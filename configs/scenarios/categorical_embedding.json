{
  "$schema": "../schemas/scenario.json",
  "name": "categorical_embedding",
  "description": "Categorical features with moderate cardinality (5 features with 20 levels each) for embedding experiments",
  "n_samples": 1000,
  "n_features": 20,
  "n_predictive": 5,
  "covariate_type": "categorical",
  "n_categorical_features": 5,
  "cardinality": 20,
  "censoring_rate": 0.3,
  "weibull_scale": 0.5,
  "weibull_shape": 2.0,
  "coefficient_range": [-1.0, 1.0]
}
